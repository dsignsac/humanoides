---
import Layout from "../layouts/Layout.astro";
import AnimatedLogo from "../components/AnimatedLogo.astro";
---

<Layout
    title="BAJO CONSTRUCCIÓN // HUMAN_SYNC"
    showNavbar={false}
    showFooter={false}
>
    <main
        class="w-full h-[100dvh] bg-brand-gray flex flex-col items-center justify-center overflow-hidden"
    >
        <div
            class="relative z-10 text-center w-full max-w-[320px] md:max-w-[480px] px-6"
        >
            <!-- Full Logo Container -->
            <div class="w-full opacity-90">
                <AnimatedLogo
                    mode="static"
                    variant="full"
                    align="center"
                    color="var(--color-brand-red)"
                />
            </div>
        </div>
    </main>
</Layout>

<style>
    /* Minimalist sober style */
    main {
        background-color: var(--color-brand-gray);
    }
</style>
