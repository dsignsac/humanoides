---
const testimonials = [
  {
    author: "MARK_DAVIDSON",
    company: "MELBOURNE_PISCO",
    role: "FOUNDER_&_CEO",
    status: "VERIFIED_PARTNER",
    log_id: "LOG_AU_772",
    feedback: "Humanoides no solo diseñó nuestra marca; sincronizó nuestra operativa global. La transición al mercado australiano fue impecable gracias a su visión técnica y estética."
  },
  {
    author: "ELENA_RIVAS",
    company: "DATASTREAM_INC",
    role: "HEAD_OF_OPERATIONS",
    status: "AUDITED_CLIENT",
    log_id: "LOG_PE_104",
    feedback: "Sistemas robustos y una dirección de arte que entiende el lenguaje corporativo moderno. Su capacidad para simplificar procesos complejos es su mayor activo."
  },
  {
    author: "JULIÁN_CASTRO",
    company: "LA_CARNICERÍA",
    role: "CREATIVE_DIRECTOR",
    status: "LEGACY_PARTNER",
    log_id: "LOG_PE_019",
    feedback: "Disrupción con sentido. Lograron que una galería de arte operara con la precisión de una industria cárnica. Un trabajo de branding visceral y técnico."
  }
];
---

<section id="testimonios" class="py-24 px-6 bg-brand-salmon border-b border-brand-black/10 relative overflow-hidden">
  <div class="max-w-[1440px] mx-auto">
    <div class="mb-16 space-y-4">
      <h2 class="text-xs font-bold tracking-[0.5em] text-brand-red uppercase">05. FEEDBACK_REGISTRY_AUDIT_2026</h2>
      <h3 class="text-4xl md:text-7xl font-800 tracking-tighter text-brand-black uppercase leading-[0.85]">
        Registros de<br/><span class="text-brand-black/20 italic">Sincronización Contentas</span>
      </h3>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-px bg-brand-black/10 border border-brand-black/10">
      {testimonials.map((t) => (
        <div class="bg-brand-salmon p-10 flex flex-col justify-between group hover:bg-white transition-colors duration-500 relative overflow-hidden">
          <div class="relative z-10">
            <div class="flex justify-between items-start mb-8">
              <span class="text-[10px] font-mono text-brand-red font-bold uppercase tracking-widest">[{t.log_id}]</span>
              <div class="px-2 py-0.5 border border-brand-red text-brand-red text-[8px] font-bold tracking-widest">
                STATUS: {t.status}
              </div>
            </div>
            
            <p class="text-xl md:text-2xl font-100 italic leading-tight text-brand-black/80 mb-12">
              "{t.feedback}"
            </p>
          </div>

          <div class="relative z-10 border-t border-brand-black/5 pt-8">
            <h4 class="text-lg font-800 tracking-tighter text-brand-red uppercase">{t.author}</h4>
            <div class="flex flex-col gap-1">
                <span class="text-[10px] font-mono tracking-widest opacity-40 uppercase">{t.company}</span>
                <span class="text-[10px] font-mono tracking-widest opacity-40 uppercase">{t.role}</span>
            </div>
          </div>

          <!-- Industrial background pattern hover -->
          <div class="absolute -bottom-4 -right-4 text-[12rem] font-800 text-brand-red/5 select-none pointer-events-none transition-transform group-hover:-translate-y-4">
            "
          </div>
        </div>
      ))}
    </div>

    <div class="mt-16 flex justify-between items-end">
        <div class="text-[8px] font-mono opacity-30 uppercase leading-relaxed">
            Sistemas de Validación Humanoides & Asociados<br/>
            Ref: AUD-LOG-2026-GLOBAL-SYNCHRONIZATION
        </div>
        <div class="flex gap-1">
            {[1,2,3,4,5].map(() => <div class="w-1 h-3 bg-brand-red opacity-20"></div>)}
        </div>
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  const initTestimonials = () => {
    gsap.from("#testimonios .bg-brand-salmon", {
      opacity: 0,
      y: 40,
      duration: 1,
      stagger: 0.2,
      ease: "power2.out",
      scrollTrigger: {
        trigger: "#testimonios",
        start: "top 70%"
      }
    });

    // Subtle flicker on entrance for lore
    gsap.to("#testimonios h3", {
      opacity: 0.8,
      duration: 0.1,
      repeat: 3,
      yoyo: true,
      scrollTrigger: {
        trigger: "#testimonios",
        start: "top 70%"
      }
    });
  };

  document.addEventListener("DOMContentLoaded", initTestimonials);
  document.addEventListener("astro:after-swap", initTestimonials);
</script>
