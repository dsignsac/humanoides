---
import Layout from "../layouts/Layout.astro";
import AnimatedLogo from "../components/AnimatedLogo.astro";
---

<Layout title="Core Identity | Humanoides & Asociados">
    <main
        class="min-h-screen bg-brand-gray flex flex-col gap-16 md:gap-32 pt-24 md:pt-32"
    >
        <!-- Header -->
        <header class="max-w-[1240px] mx-auto w-full px-6 md:px-12">
            <div class="w-full">
                <h1
                    class="text-6xl md:text-9xl font-sans mb-6 tracking-tighter text-brand-red leading-[0.85]"
                >
                    Core Team
                </h1>
                <p
                    class="text-xl md:text-3xl opacity-90 max-w-3xl text-brand-black leading-tight"
                >
                    El motor humano y digital detrás de la eficiencia
                    disruptiva.
                </p>
            </div>

            <!-- Manifiesto -->
            <div
                class="mt-20 md:mt-32 border-l-[6px] border-brand-red pl-8 md:pl-16 py-4 group"
            >
                <p
                    class="text-3xl md:text-6xl font-sans text-brand-black leading-[1.05] w-full tracking-tight mb-8"
                >
                    "Somos un Director de Arte y sus agentes. Listos para
                    planear, diseñar y construir. Estamos aquí para inyectar
                    eficiencia disruptiva a negocios que buscan sistemas
                    eficientes y escalables de toda índole digital."
                </p>
                <div class="flex flex-col h-auto">
                    <p
                        class="text-xl md:text-2xl font-sans font-bold text-brand-black/90 leading-none"
                    >
                        David Salcedo
                    </p>
                    <p
                        class="text-brand-red font-mono text-[10px] md:text-xs uppercase tracking-[0.4em] opacity-80 mt-1"
                    >
                        Humanoide
                    </p>
                </div>
            </div>
        </header>

        <!-- 0. Partners Carousel -->
        <section
            class="w-full overflow-hidden py-12 border-y border-black/5 bg-white/10 backdrop-blur-sm"
        >
            <div class="max-w-[1240px] mx-auto px-6 md:px-12 mb-10">
                <h2
                    class="text-xs uppercase tracking-[0.3em] opacity-50 font-mono"
                >
                    01. Strategic Partners
                </h2>
            </div>

            <!-- Infinite Carousel Container -->
            <div class="flex w-full overflow-hidden group">
                <div
                    class="flex animate-scroll whitespace-nowrap gap-16 md:gap-32 items-center py-4"
                >
                    {
                        [...Array(2)].map(() => (
                            <>
                                {[
                                    "stripe.com",
                                    "vercel.com",
                                    "github.com",
                                    "openai.com",
                                    "anthropic.com",
                                    "google.com",
                                    "figma.com",
                                    "adobe.com",
                                ].map((domain) => (
                                    <div class="flex-shrink-0 grayscale opacity-40 hover:opacity-100 hover:grayscale-0 transition-all duration-500 cursor-crosshair">
                                        <img
                                            src={`https://logo.clearbit.com/${domain}?size=200`}
                                            alt={domain}
                                            class="h-8 md:h-12 w-auto object-contain"
                                        />
                                    </div>
                                ))}
                            </>
                        ))
                    }
                </div>
            </div>
        </section>

        <!-- 0.1 Monthly Recognition -->
        <section class="max-w-[1240px] mx-auto w-full px-6 md:px-12 mb-40">
            <h2
                class="text-xs uppercase tracking-[0.3em] opacity-50 mb-10 font-mono"
            >
                02. Top Performance
            </h2>
            <div
                class="bg-brand-black rounded-[3rem] p-8 md:p-16 flex flex-col md:flex-row gap-12 items-center overflow-hidden relative group"
            >
                <!-- Decorative background -->
                <div
                    class="absolute top-0 right-0 w-1/2 h-full bg-gradient-to-l from-brand-red/10 to-transparent pointer-events-none"
                >
                </div>

                <div class="w-32 md:w-48 lg:w-64 relative z-10 shrink-0">
                    <div
                        class="aspect-square rounded-full bg-white/10 p-6 border border-white/10 flex items-center justify-center backdrop-blur-md"
                    >
                        <img
                            src="https://upload.wikimedia.org/wikipedia/commons/2/22/Google_Gemini_icon_2025.svg"
                            alt="Gemini AI"
                            class="w-full h-full object-contain"
                        />
                    </div>
                    <div
                        class="absolute -bottom-4 -right-4 bg-brand-red text-white font-mono text-[10px] px-3 py-1 rounded-full animate-pulse shadow-lg"
                    >
                        TOP AGENT
                    </div>
                </div>

                <div class="flex flex-col gap-6 relative z-10">
                    <div class="space-y-2">
                        <span
                            class="text-brand-red font-mono text-xs uppercase tracking-widest block font-bold"
                            >Reconocimiento Mensual</span
                        >
                        <h3
                            class="text-4xl md:text-7xl font-sans font-bold text-white tracking-tighter"
                        >
                            Google Gemini AI
                        </h3>
                    </div>
                    <p
                        class="text-white/60 text-lg md:text-2xl font-sans leading-snug max-w-2xl italic"
                    >
                        "Su excelencia en lógica computacional y asistencia
                        creativa redefine los límites de lo que nuestros agentes
                        pueden lograr."
                    </p>
                    <div class="flex flex-wrap gap-4 mt-4">
                        <div
                            class="px-5 py-2 rounded-xl bg-white/5 border border-white/10 text-xs font-mono text-white/50 uppercase tracking-widest"
                        >
                            Non-Stop Logic
                        </div>
                        <div
                            class="px-5 py-2 rounded-xl bg-white/5 border border-white/10 text-xs font-mono text-white/50 uppercase tracking-widest"
                        >
                            Deep Reasoning
                        </div>
                        <div
                            class="px-5 py-2 rounded-xl bg-white/5 border border-white/10 text-xs font-mono text-white/50 uppercase tracking-widest font-bold text-brand-red/70"
                        >
                            Core Certified
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>
</Layout>

<style>
    @keyframes scroll {
        0% {
            transform: translateX(0);
        }
        100% {
            transform: translateX(-50%);
        }
    }
    .animate-scroll {
        animation: scroll 40s linear infinite;
        width: max-content;
    }
    .animate-scroll:hover {
        animation-play-state: paused;
    }
</style>
